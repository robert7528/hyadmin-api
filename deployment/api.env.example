# /etc/hyadmin/api.env
# 複製並填入實際值：
#   sudo mkdir -p /etc/hyadmin
#   sudo cp api.env.example /etc/hyadmin/api.env
#   sudo chmod 600 /etc/hyadmin/api.env

SERVER_PORT=8080
SERVER_MODE=release
DATABASE_DSN=host=host-gateway user=hysp password=CHANGE_ME dbname=hysp port=5432 sslmode=disable
LOG_LEVEL=info
LOG_FILENAME=

# JWT (required in production)
JWT_SECRET=CHANGE_ME_USE_STRONG_RANDOM_SECRET
JWT_EXPIRY_HOURS=24

# Tink AES-GCM keyset (JSON). Generate with tinkey:
#   tinkey create-keyset --key-template AES256_GCM
# Leave empty to disable PII encryption (dev only)
TINK_KEYSET=
